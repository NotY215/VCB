export function i32 main() {
entry:
    %1 = add i32 0, 0
    jmp check
check:
    %2 = phi i32 [ %1, entry ], [ %5, body ]
    %3 = clt i32 %2, 5
    jnz %3, body, done
body:
    %5 = add i32 %2, 1
    jmp check
done:
    ret i32 %2
}